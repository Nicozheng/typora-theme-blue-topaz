# Project Preferences

- Prefer using parallel subagents for implementation work when tasks can be split into independent write scopes.
- Keep subagent write scopes explicit and disjoint, and remind workers not to revert or overwrite each other's changes.
